import React, { Component } from "react";
import styled from "styled-components";

const Container = styled.div`
  height: 75px;
  background-color: #7300e3;
`;

export default class Header extends Component {
  render() {
    return <Container></Container>;
  }
}
